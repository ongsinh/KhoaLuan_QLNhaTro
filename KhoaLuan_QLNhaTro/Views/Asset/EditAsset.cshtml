@* @model KhoaLuan_QLNhaTro.Models.Asset
<h2>Cập nhật tài sản</h2>

<form method="post">
    @Html.AntiForgeryToken()

    <div class="form-group">
        <label for="Name">Tên tài sản</label>
        <input type="text" id="Name" name="Asset.Name" class="form-control" value="@Model.Asset.Name" required />
    </div>

    <div class="form-group">
        <label for="Price">Giá</label>
        <input type="number" id="Price" name="Asset.Price" class="form-control" value="@Model.Asset.Price" required />
    </div>

    <div class="form-group">
        <label for="Status">Trạng thái</label>
        <input type="text" id="Status" name="Asset.Status" class="form-control" value="@Model.Asset.Status" required />
    </div>

    <div class="form-group">
        <label for="RoomId">Chọn phòng</label>
        <select id="editRoom" name="RoomId" class="form-control" required>
            @if (ViewBag.Rooms != null)
            {
                var rooms = ViewBag.Rooms as List<KhoaLuan_QLNhaTro.Models.Room>;
                if (rooms != null && rooms.Any())
                {
                    @foreach (var room in rooms)
                    {
                        // Kiểm tra xem RoomId có phải là Room.Id hiện tại không
                        if (room.Id == Model.Asset.RoomId)
                        {
                            <option value="@room.Id" selected>@room.Name</option>
                            // Nếu là phòng đã chọn, thêm 'selected'
                        }
                        else
                        {
                            <option value="@room.Id">@room.Name</option>
                            // Các phòng còn lại
                        }
                    }
                }
                else
                {
                    <option disabled>Không có phòng nào.</option>
                }
            }
            else
            {
                <option disabled>Không có phòng nào.</option>
            }
        </select>
    </div>

    <!-- Trường ẩn để gửi HouseId -->
    <input type="hidden" name="HouseId" value="@ViewBag.IdHouse" />

    <button type="submit" class="btn btn-primary">Lưu</button>
    <button type="button" class="close-modal" onclick="closeModal('editModal')">Đóng</button>
</form> *@

@model KhoaLuan_QLNhaTro.Models.Asset

<form method="post" asp-action="EditAsset" id="editAssetForm">
    <input type="hidden" name="HouseId" value="@ViewBag.HouseId" />
    <!-- Tên tài sản -->
    <label for="editAssetName">Tên tài sản</label>
    <input type="text" id="editAssetName" name="Name" value="@Model.Name" required>

    <!-- Giá trị tài sản -->
    <label for="editAssetValue">Giá trị tài sản</label>
    <input type="text" id="editAssetValue" name="Price" value="@Model.Price" required>

    <!-- Trạng thái -->
    <label for="editStatus">Trạng thái</label>
    <input type="text" id="editStatus" name="Status" value="@Model.Status" required>

    <!-- Chọn phòng -->
    <label for="editRoom">Chọn phòng</label>
    
    <select id="editRoom" name="RoomId" class="form-control" required>
        @if (ViewBag.Rooms != null)
        {
            var rooms = ViewBag.Rooms as List<KhoaLuan_QLNhaTro.Models.Room>;
            if (rooms != null && rooms.Any())
            {
                @foreach (var room in rooms)
                {
                    // Kiểm tra xem RoomId có phải là Room.Id hiện tại không
                    if (room.Id == Model.RoomId)
                    {
                        <option value="@room.Id" selected>@room.Name</option>
                        // Nếu là phòng đã chọn, thêm 'selected'
                    }
                    else
                    {
                        <option value="@room.Id">@room.Name</option>
                        // Các phòng còn lại
                    }
                }
            }
            else
            {
                <option disabled>Không có phòng nào.</option>
            }
        }
        else
        {
            <option disabled>Không có phòng nào.</option>
        }
    </select>


    <!-- Trường AssetId ẩn -->
    <input type="hidden" id="editAssetId" name="Id" value="@Model.Id">

    <button type="submit" class="submit-modal">Lưu thay đổi</button>
    <button type="button" class="close-modal" onclick="closeModal('editModal')">Đóng</button>
</form>
